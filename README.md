# Uninformed_search_using_python
It  Involves the Implementation of  BFS, DFS, and IDDFS on a tree graph using Python. The diagram was modeled as an adjacency list, algorithms executed from the root, and IDDFS used a depth limit. Outputs showed each method’s unique traversal strategy.
